# 完整全站改造指令（专业高级官网风格）

---

## 一、全局设计令牌（必须遵守）

- **主背景**：`radial-gradient(circle at 10% 20%, #0a0f1c, #030614)`，固定，全屏覆盖。
- **字体**：
  - 英文：`'Inter', 'SF Pro', system-ui, sans-serif`
  - 中文：`'PingFang SC', 'Microsoft YaHei', sans-serif`
  - 代码装饰：`'JetBrains Mono', monospace`
- **主色调**：`#3b82f6`（电光蓝）
- **文字颜色**：
  - 主要（标题、重要文字）：`#ffffff`
  - 次要（描述、正文）：`#cbd5e1`
  - 辅助（标签、小字）：`#94a3b8`
- **边框/分割线**：`#1e293b` 或 `rgba(59,130,246,0.2)`
- **卡片背景**：`rgba(15, 23, 42, 0.6)`，带 `backdrop-filter: blur(4px)`，圆角 `16px`。
- **过渡动画**：`0.2s ease-out`
- **页面最大宽度**：`1200px`，左右居中，内边距 `20px`（移动端 `16px`）。

---

## 二、页面结构（单页滚动，锚点导航）

网站包含以下三个区块：
1. **首页（Hero）** – id="home"
2. **作品展示** – id="projects"
3. **联系我** – id="contact"

导航栏点击对应菜单项平滑滚动到相应区块。

---

## 三、顶部导航栏（重构）

### 3.1 样式与行为
- 固定在页面顶部 `position: fixed; top: 0; left: 0; width: 100%; z-index: 1000;`
- 初始背景：透明。
- 滚动超过 50px 后：背景 `rgba(3, 6, 20, 0.8)`，`backdrop-filter: blur(12px)`。
- 高度：`72px`。
- 内容区：`max-width: 1200px`，左右居中，flex 布局，两端对齐。
- 左右内边距：`20px`。

### 3.2 左侧品牌标识
- 内容：`<DEV/>`（使用等宽字体，字号 `20px`，字重 `500`，颜色 `#60a5fa`）。
- hover 颜色变为 `#3b82f6`，无下划线。
- 点击滚动到首页。

### 3.3 右侧导航菜单（桌面端）
- 菜单项：`首页`、`作品`、`关于我`、`联系我`。
- 水平排列，间距 `32px`。
- 样式：字号 `16px`，颜色 `#e2e8f0`，无下划线。
- hover 颜色 `#3b82f6`。
- 激活项（当前视口所在区块）：文字加粗，并添加一个 `2px` 高的蓝色下划线（宽度与文字相同，使用 `::after` 伪元素）。
- 移动端（宽度 < 768px）隐藏此菜单，显示汉堡按钮。

### 3.4 移动端汉堡菜单
- 按钮：`☰`，字号 `24px`，颜色 `#e2e8f0`，点击展开侧边抽屉。
- 侧边抽屉：从左侧滑入，宽度 `70%`，背景 `rgba(10, 15, 28, 0.95)`，`backdrop-filter: blur(16px)`，内边距 `24px`。
- 菜单项垂直排列，间距 `24px`，字号 `18px`。
- 点击菜单项后关闭抽屉并滚动到对应区块。
- 抽屉外部有半透明遮罩层，点击遮罩关闭。

---

## 四、首页（Hero）区块

### 4.1 布局
- 桌面端：左右两栏（左侧 50%，右侧 40%），垂直居中。
- 移动端：右侧图形隐藏，左侧内容居中，内边距 `20px`。
- 为补偿固定导航栏，给 Hero 容器添加 `padding-top: 72px` 或使用 `scroll-margin-top: 72px`。

### 4.2 左侧内容（从上到下）
1. **小标题**：`<DEV/>` 或 `@Autowired`，字号 `14px`，颜色 `#60a5fa`，字母间距 `2px`，等宽字体。
2. **大标题**：`能够帮您把想法变成可靠系统`，字号 `56px~72px`（响应式），字重 `700`，颜色白色，行高 `1.2`。
3. **描述文字**：`深耕全栈架构与系统设计5年，同时热衷探索AI提效与变现。相信好的代码和好的生成结果一样，细节决定质感。追求逻辑的严谨与技术的温度，平衡代码的确定性与AI的可能性，在技术浪潮里走自己的路。` 字号 `18px`，行高 `1.6`，颜色 `#cbd5e1`，最大宽度 `560px`。
4. **CTA 按钮组**：
   - 主按钮：“联系我”，背景 `#3b82f6`，文字白色，内边距 `12px 32px`，圆角 `8px`，hover 背景 `#2563eb` 且 `transform: scale(1.02)`。
   - 次按钮：“查看作品”，背景透明，边框 `2px solid #3b82f6`，文字 `#3b82f6`，内边距 `12px 32px`，圆角 `8px`，hover 背景 `rgba(59,130,246,0.1)`。
   - 按钮间距 `20px`，移动端垂直排列，宽度 `100%`。
5. **滚动提示**：在 Hero 底部中央放置向下箭头（`↓`），颜色 `#94a3b8`，大小 `24px`，添加上下跳动动画（每 2 秒一次），点击平滑滚动到作品区块。

### 4.3 右侧图形
- 使用 Three.js 创建一个极简的环面结（`TorusKnotGeometry`）或线框球体，颜色 `#3b82f6`，透明度 `0.6`，线框材质，缓慢自转（`rotation.y += 0.005`）。
- 仅桌面端加载，移动端不加载（通过媒体查询或 JS 检测屏幕宽度）。

### 4.4 背景细节
- **代码关键词纹理**：在全局背景上极淡随机显示 Java/Spring 关键词（例如 `@Service`, `@RestController`, `ApplicationContext`, `Stream`, `CompletableFuture`），颜色 `#1e293b`，透明度 `0.15`，字号 `12px`，不仔细看不可见。仅作为背景装饰，不干扰阅读。
- **网格线**：叠加极细网格线（`background-image: repeating-linear-gradient(...)`），透明度 `0.05`。
- **光晕**：左上角（`#3b82f6`）和右下角（`#8b5cf6`）各一个模糊圆形，半径 `150px`，透明度 `0.2`。

### 4.5 动画与交互
- 页面加载：小标题、大标题、描述、按钮依次从下方淡入上浮，间隔 `0.1s`，时长 `0.6s`。
- 光标跟随光晕：桌面端 Hero 区域移动鼠标时，一个模糊圆形光晕（主色，半径 `80px`，透明度 `0.3`）跟随鼠标。
- 尊重 `prefers-reduced-motion`：简化动画（仅保留淡入，取消跟随光晕和旋转）。

---

## 五、作品展示区块

### 5.1 区块标题
- `精选项目`，字号 `36px`，字重 `600`，颜色白色，居中或左对齐。
- 下方一条 `60px` 宽蓝色下划线（`#3b82f6`），高度 `2px`，居中。

### 5.2 项目卡片网格
- 桌面端：`2~3` 列，间距 `24px`。
- 移动端：`1` 列，间距 `20px`。
- 卡片样式：
  - 背景 `rgba(15, 23, 42, 0.6)`，边框 `1px solid rgba(59,130,246,0.2)`，圆角 `16px`，内边距 `24px`。
  - hover：边框 `#3b82f6`，阴影 `0 20px 25px -12px rgba(0,0,0,0.5)`。
- 每个卡片内容：
  - 标题（如 `闲鱼管理系统`），字号 `24px`，字重 `600`，白色。
  - 简短描述（如 `高效专业的闲鱼自动化管理平台`），字号 `16px`，颜色 `#94a3b8`，行高 `1.5`。
  - 功能标签列表（如 `自动回复`、`智能客服`、`订单管理`、`数据分析`），每个标签：背景 `#1e293b`，文字 `#60a5fa`，内边距 `4px 12px`，字号 `12px`，圆角 `20px`，间距 `8px`。
  - 年份标签（如 `2025`），置于卡片右上角（绝对定位或 flex 右对齐），字号 `12px`，颜色 `#64748b`。
  - 技术栈图标（可选）：显示 React、Spring Boot 等小图标（使用 emoji 或字体图标）。
  - 查看详情链接：`查看详情 →`，颜色 `#3b82f6`，hover 下划线，点击可打开新窗口或弹框（当前先做 `alert('演示模式')`）。

### 5.3 至少三个真实项目（基于你的简历）
1. **闲鱼管理系统**：AI 提效工具，自动回复、智能客服、订单管理、数据分析。技术栈：Spring Boot + Vue。
2. **营销自动化系统 (MA)**：事件驱动架构，集群吞吐量提升 40%，任务失败率下降 70%。技术栈：Spring Cloud + Kafka + Redis。
3. **智能代码评审助手**：基于本地大模型（Qwen + deepseek-coder），GitLab MR 自动审查，RAG + 知识图谱。技术栈：Spring AI + Ollama + Neo4j。

### 5.4 删除所有无关元素
- 不要显示任何登录表单（用户名、密码、记住登录等）。
- 不要显示 CodePal 相关内容。
- 不要显示额外版权信息（统一放在页脚）。

---

## 六、关于我区块

- 标题：`关于我`，样式同作品区块。
- 内容：可复用联系页左侧的引导语和个人简介。建议展示：
  - 简短自我介绍（100字左右）。
  - 核心技能标签（Java, Spring Cloud, AI, 高并发等）。
  - 生活态度（热爱技术，也热爱生活）。
- 为了简洁，也可以直接链接到联系页的左侧信息，但建议单独列出。

---

## 七、联系我区块

### 7.1 布局
- 桌面端：左右两栏（左侧 45%，右侧 45%），移动端堆叠。

### 7.2 左侧（联系方式）
- 引导语：`我始终对具有挑战性的项目与有趣的创意充满期待。如果您有任何想法或合作意向，欢迎随时联系我。` 字号 `18px`，颜色 `#cbd5e1`，行高 `1.6`，margin-bottom `32px`。
- 联系方式列表（图标 + 文字，垂直排列，间距 `24px`）：
  - 📧 邮箱：`huangxin981230@163.com`，旁边加复制按钮（点击复制邮箱，显示“已复制”提示，2秒后消失）。
  - 💬 微信：`hx95152437`，下方小字提示：`添加好友请备注“合作”`（颜色 `#64748b`）。
  - 📞 电话：`+86 18805691256`。
- 社交链接：GitHub、头条、公众号，用小图标（或文字）排列，间距 `16px`，hover 变主色。

### 7.3 右侧（联系表单）
- 字段：
  - 姓名（可选）：`placeholder="你的姓名"`。
  - 邮箱（必填）：`type="email"`，`placeholder="your@email.com"`。
  - 消息内容（必填）：`textarea`，`rows=4`，`placeholder="请详细描述您的需求..."`。
- 输入框样式：背景 `#1e293b`，边框 `#334155`，圆角 `8px`，内边距 `12px`，颜色白色，focus 边框 `#3b82f6`，outline 无。
- 提交按钮：主色填充，hover 加深，文字白色，内边距 `12px 24px`，圆角 `8px`。
- 表单下方提示：`我会在 24 小时内回复。`，字号 `12px`，颜色 `#64748b`。
- 提交逻辑（UI 演示）：点击按钮后 `alert('演示模式：您的消息已发送，我会尽快回复。')`，不清空表单（或清空，由你决定）。

### 7.4 删除冗余
- 不要出现 `邮箱联系` `微信咨询` `电话咨询` 等旧按钮。
- 不要出现 `XINXINNOTE.TECH` 和 `快速链接` / `联系我们` 区块（除非放在页脚）。

---

## 八、统一页脚

- 背景：`#0a0f1c`，上方 `1px` 边框 `#1e293b`，内边距 `32px 20px`。
- 内容（桌面端两端对齐，移动端居中）：
  - 第一行左侧：导航链接（首页、作品、关于我、联系），间距 `24px`，颜色 `#94a3b8`，hover 变主色。
  - 第一行右侧：社交图标（GitHub、头条、公众号），间距 `16px`（可用文字或图标）。
  - 第二行：版权信息 `© 2026 黄鑫 (Frank). Built with React + TypeScript + Vite `，字号 `12px`，颜色 `#64748b`，居中。
- 回到顶部按钮：右下角悬浮圆形按钮，背景 `#3b82f6`，白色箭头，点击平滑滚动至 `window.scrollTo({ top: 0, behavior: 'smooth' })`，滚动超过 300px 时显示。

---

## 九、响应式与性能要求

- 移动端（<768px）：
  - 首页右侧 Three.js 图形隐藏。
  - 大标题字号 `36px`，描述 `16px`。
  - 按钮垂直排列，宽度 `100%`。
  - 作品网格 `1` 列，卡片内边距 `16px`。
  - 联系页两栏堆叠。
- 低性能设备或 `prefers-reduced-motion`：关闭光标跟随光晕、关闭 Three.js 旋转、减少入场动画（仅保留淡入）。
- Three.js 仅桌面端加载，且使用 `requestAnimationFrame` 时注意在移动端不创建场景。

---

## 十、完整代码输出要求

- 输出一个完整的代码
- 使用现代浏览器特性（ES6、CSS Grid/Flex）。
- 添加必要的 meta viewport 设置。
- 确保所有交互（导航滚动、表单 alert、复制邮箱、回到顶部）都能正常工作。
- 不要遗漏任何模块（导航、首页、作品、关于我、联系、页脚）。
- 注意：由于没有提供真实后端，表单提交使用 `alert` 演示即可。
- 代码必须清晰、注释得当，便于二次修改。

---

请严格按照以上指令生成代码。